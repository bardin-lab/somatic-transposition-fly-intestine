To run the notebooks first install conda. An environemnt with the necessary software can then be created using

```console
conda env create -f requirements.yml
conda activate somatic-transposition-fly-intestine
```
